# Vite environment variables (must be prefixed VITE_ to be exposed to the client).
# In dev, the Vite proxy forwards /healthz and /api to the backend on :5000;
# this var is only relevant for direct backend addressing (e.g. preview builds).
VITE_API_BASE=http://localhost:5000
